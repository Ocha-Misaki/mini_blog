= form_with(model: current_user.active_relationships.find_by(followed: @user),
html: { method: :delete }, url: user_relationship_path) do |f|
  =hidden_field_tag :followed_id, user.id
  =f.submit 'フォロー解除する', class: "btn"
