.row
  %section.micopost_form
    = form_with(model: [:user, @micropost]) do |f|
      = f.label :content
      = f.text_area :content
      = f.submit "Update", class: "btn btn-primary"

  = link_to '戻る', user_profile_path
