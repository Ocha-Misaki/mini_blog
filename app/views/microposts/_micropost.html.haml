%li
  %span.user
    = link_to micropost.user.name, user_path(micropost.user)
  %span.content
    = micropost.content
  %span.timestamp
    = time_ago_in_words(micropost.created_at) + '前'
  %span.edit
    = link_to '詳細', user_micropost_path(micropost)
